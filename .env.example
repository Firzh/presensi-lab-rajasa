DB_PASSWORD=
DB_NAME=myapp_db

# Frontend Vite dev server exposed to host
FRONTEND_PORT=3000

# Nginx entrypoint for API routing: http://localhost:8080/api/...
NGINX_PORT=8080

# Linux/macOS:
# Set these values to match your host user:
# UID=$(id -u)
# GID=$(id -g)
UID=1000
GID=1000